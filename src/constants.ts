// Inputs
export const INPUT_EXPIRE_IN_DAYS = 'expire-in-days';
export const INPUT_TAG_PREFIX = 'tag-prefix';
export const DEFAULT_TAG_PREFIX = 'v';

// Outputs
export const OUTPUT_VERSION = 'version';
export const OUTPUT_STABLE = 'stable';
